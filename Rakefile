task :default do
  puts "Remote GSM"
  puts "Available commands:"
  puts "start['COMX']     - Start Remote GSM at COM Port X (e.g., COM4)"
  puts "healtcheck        - Check for necessary binary dependencies, and advise where to download"
end

task :start => [:healthcheck] do 
  
end

task :healthcheck do
  #TODO check for necessary files
end